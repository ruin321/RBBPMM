<Window x:Class="RBBPMM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:RBBPMM.ViewModels"
        xmlns:views="clr-namespace:RBBPMM.Views"
        xmlns:easter="clr-namespace:RBBPMM.EasterEggs"
        xmlns:loc="clr-namespace:RBBPMM.Localization"
        mc:Ignorable="d"
        Title="{loc:Loc Key=app.title}"
        Height="760" Width="1180"
        Background="{DynamicResource BgBrush}" Foreground="{DynamicResource TextBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 侧边栏 -->
        <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top" Margin="0,8">
                    <TextBlock Text="RBBPMM" FontSize="20" FontWeight="Bold"
                               Margin="16,8,16,2" Foreground="{DynamicResource PrimaryBrush}"/>
                    <TextBlock Text="{loc:Loc Key=app.subtitle}" FontSize="11" Margin="16,0,16,8"
                               Foreground="{DynamicResource TextMutedBrush}"/>
                </StackPanel>

                <ItemsControl ItemsSource="{Binding NavItems}" DockPanel.Dock="Top">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding Key}"
                                    Margin="8,3" Padding="12,9" HorizontalContentAlignment="Left"
                                    HorizontalAlignment="Stretch" BorderThickness="0"
                                    Background="{DynamicResource SurfaceBrush}"
                                    Foreground="{DynamicResource TextBrush}" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                                                            CornerRadius="{DynamicResource RadiusSm}"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <!-- 当前页高亮：IsActive 由 MainViewModel 在导航时刷新
                                                             （DataTrigger.Value 不支持 Binding，故走布尔量） -->
                                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                            <Setter TargetName="bd" Property="Background"
                                                                    Value="{DynamicResource NavActiveBrush}"/>
                                                        </DataTrigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Icon}" FontSize="15" Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding Title}" FontSize="13"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>
        </Border>

        <!-- 内容区 -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1"
                    Padding="14,10">
                <DockPanel LastChildFill="False">
                    <TextBlock DockPanel.Dock="Left" FontSize="16" FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Text="{Binding Nav.CurrentPage.Title, FallbackValue=''}"
                               Foreground="{DynamicResource TextBrush}"/>
                    <Button DockPanel.Dock="Right" Content="{Binding ThemeGlyph}"
                            Command="{Binding ToggleThemeCommand}"
                            Padding="10,5" MinWidth="42" ToolTip="{loc:Loc Key=shell.toggleTheme}"
                            Style="{StaticResource BaseButton}"/>
                </DockPanel>
            </Border>

            <ContentControl x:Name="PageHost" Grid.Row="1" Margin="14" Content="{Binding Nav.CurrentPage}">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type vm:ModsPageViewModel}">
                        <views:ModsPage/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:TexturesPageViewModel}">
                        <views:TexturesPage/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:LevelsPageViewModel}">
                        <views:LevelsPage/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:GameBananaPageViewModel}">
                        <views:GameBananaPage/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:SettingsPageViewModel}">
                        <views:SettingsPage/>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </Grid>

        <!-- 彩蛋层：铺满整窗（突脸要盖住侧边栏），但内部按钮对齐内容区，不压标题栏。
             背景为 null 所以不吃点击，只有彩蛋自己的按钮/遮罩参与命中测试。 -->
        <easter:EggLayer Grid.Column="0" Grid.ColumnSpan="2"
                         Panel.ZIndex="100"
                         FieldTarget="{Binding ElementName=PageHost}"
                         Eggs="{Binding Eggs}"/>
    </Grid>
</Window>
